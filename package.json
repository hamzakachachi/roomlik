{
  "name": "roomlik-video-calls",
  "version": "1.0.0",
  "private": true,
  "description": "Roomlik — password-protected, two-person WebRTC video rooms.",
  "author": "Hamza Kachachi",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check src/create-server.js && node --check public/app.js && node --check public/coming-soon.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "dotenv": "^17.2.3",
    "express": "^4.21.2",
    "helmet": "^8.1.0",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
